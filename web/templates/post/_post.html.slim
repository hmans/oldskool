article.post
  .meta = link format_time(@post.inserted_at), to: post_path(@conn, :show, @post)
  
  = if @post.title do
    h2 = link @post.title, to: post_path(@conn, :show, @post)
    
  = @post.html |> raw
